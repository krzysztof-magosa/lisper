(begin
 (define fact (lambda (n)
                (if (<= n 1)
                    1
                  (* n (fact (- n 1))))))

 (define i 1)
 (while (<= i 10)
   (begin
    (print (fact i))
    (set! i (+ i 1)))))
