(begin
 (define fib (lambda (n)
               (if (< n 2)
                   n
                 (+ (fib (- n 2)) (fib (- n 1))))))

 (define i 1)
 (while (<= i 10)
   (begin
    (print (fib i))
    (set! i (+ i 1)))))
